name: Apply VFX v1.5 Musical Macros

on:
  push:
    branches:
      - audio-wave-vfx-v1
    paths:
      - tools/apply-vfx-v1-5-musical-macros.py
      - .github/workflows/apply-vfx-v1-5-musical-macros.yaml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audio-wave-vfx-v1
          fetch-depth: 0
      - name: Apply guarded migration
        run: python3 tools/apply-vfx-v1-5-musical-macros.py
      - name: Commit source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/audio-shader-source.cpp
          git diff --cached --quiet && exit 0
          git commit -m "Add VFX v1.5 musical macro uniforms"
          git push
