name: Apply VFX v1.7 Beat Envelope

on:
  push:
    branches:
      - audio-wave-vfx-v1
    paths:
      - tools/apply-vfx-v1-7-beat-envelope.py
      - .github/workflows/apply-vfx-v1-7-beat-envelope.yaml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audio-wave-vfx-v1
          fetch-depth: 0
      - name: Apply guarded beat-envelope migration
        run: python3 tools/apply-vfx-v1-7-beat-envelope.py
      - name: Commit source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/audio-shader-source.cpp src/includes/audio-shader-source.hpp
          git diff --cached --quiet && exit 0
          git commit -m "Add independent VFX beat envelope"
          git push
