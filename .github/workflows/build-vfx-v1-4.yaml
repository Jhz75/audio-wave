name: Build VFX v1.4

on:
  push:
    branches:
      - audio-wave-vfx-v1
    paths:
      - src/audio-shader-source.cpp
      - src/includes/audio-shader-source.hpp
      - .github/workflows/build-vfx-v1-4.yaml

jobs:
  build:
    uses: ./.github/workflows/build-project.yaml
    secrets: inherit

# validation trigger: v1.4 mapping batch
