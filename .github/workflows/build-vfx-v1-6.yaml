name: Build VFX v1.6

on:
  push:
    branches:
      - audio-wave-vfx-v1
    paths:
      - src/audio-shader-source.cpp
      - src/includes/audio-shader-source.hpp
      - .github/workflows/build-vfx-v1-6.yaml

# validation trigger for VFX v1.7 independent beat envelope
jobs:
  build:
    uses: ./.github/workflows/build-project.yaml
    secrets: inherit
